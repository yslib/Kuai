@PACKAGE_INIT@

# This MODULE target locates the plugin for deployment; it is not a link target.
include("${CMAKE_CURRENT_LIST_DIR}/@vendor_package@Targets.cmake")

check_required_components(@vendor_package@)

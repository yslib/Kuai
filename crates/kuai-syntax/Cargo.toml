[package]
name = "syntax"
version = "0.1.0"
edition = "2024"

[dependencies]
lasso = "0.7.3"
logos = "0.16.0"
pretty = "0.10.0"

[dev-dependencies]
insta = { version = "1.45.0", features = ["yaml","glob"] }

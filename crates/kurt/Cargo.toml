[package]
name = "kurt"
version = "0.1.0"
edition = "2024"
description = "Rust interface to Kuai with intrusive native ownership and borrowed runtime views"

[dependencies]
kurt-sys = { path = "../kurt-sys" }

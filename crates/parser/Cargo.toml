[package]
name = "parser"
version = "0.1.0"
edition = "2024"

[dependencies]
bumpalo = "3.19.0"
lasso = "0.7.3"
logos = "0.16.0"
miette = {version= "7.6.0", features = ["fancy"]}
pretty = "0.10.0"
thiserror = "2.0.17"

[dev-dependencies]
insta = { version = "1.45.0", features = ["yaml","glob"] }

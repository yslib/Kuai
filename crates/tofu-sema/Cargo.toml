[package]
name = "sema"
version = "0.1.0"
edition = "2024"

[dependencies]
tofu_core = { path = "../tofu-core" }
syntax = { path = "../tofu-syntax" }

[dev-dependencies]
insta = { version = "1.45.0", features = ["glob"] }
